v0.12.1 docker container setup
    v0.12.1 docker container setup
    modify the .idx/dev.nix file to modify our workspace and update necessary files to make this project run in docker. we should the docker should mounted to our code. also setup other necessary images other than this (flarebee - frontend) docker image. and we should able to organize images in one docker compose and setup by running docker compose up. learn from this documentation: https://firebase.google.com/docs/studio/customize-workspace.